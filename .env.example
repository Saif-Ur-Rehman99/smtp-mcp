# Copy this to .env and fill in your values
# Never commit .env to git

RESEND_API_KEY=your_resend_api_key_here

# Transport: "sse" for cloud/Railway, "stdio" for local Claude Code
MCP_TRANSPORT=sse

# Port (Railway sets this automatically)
PORT=8000
